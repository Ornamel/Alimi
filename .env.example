# Copy this file to .env.local and fill in your Supabase project credentials.
# Without these, the app runs against a local browser-only mock backend
# (see src/lib/backend/localBackend.ts) so the UI can still be exercised end-to-end.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
